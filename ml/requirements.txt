# SIM-POC / M3 / M4 environment — verified working 2026-08-05 (PRD P1)
#
# Python 3.12.10 (3.11 is not installed on this machine; 3.12 was the
# pre-declared fallback and torch/gym-donkeycar both cover it).
#
# Rebuild:
#   py -3.12 -m venv .venv
#   .venv\Scripts\python.exe -m pip install -r ml\requirements.txt --index-url https://download.pytorch.org/whl/cu126 --extra-index-url https://pypi.org/simple
#   .venv\Scripts\python.exe ml\verify_env.py      # must print P1 DONE-CHECK: PASS
#
# The simulator binary is NOT a pip package — download DonkeySimWin.zip from
# https://github.com/tawnkramer/gym-donkeycar/releases/tag/v25.10.06
# (236,059,289 bytes) and extract to sim/. It is gitignored.

# CUDA build — the cu126 index URL above is required, or pip installs CPU-only torch
torch==2.13.0+cu126

numpy==2.5.1
pillow==12.3.0
gymnasium==1.3.0

# NOT from PyPI: PyPI's gym-donkeycar is 1.0.13 from 2019-08-04, six years
# stale and mismatched with the 2025 simulator. Pinned to the commit verified
# working on 2026-08-05 (release v25.10.06, package version.txt says 1.3.1).
gym_donkeycar @ git+https://github.com/tawnkramer/gym-donkeycar.git@a1f4ca6961e17a6929c1f2e883358a1edafb479d
